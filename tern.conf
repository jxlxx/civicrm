[database]
# Database connection settings using standard PostgreSQL environment variables
# These will be automatically picked up from the environment
# No need to hardcode values - just set the environment variables

# Standard PostgreSQL environment variables:
# PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD, PGSSLMODE

# Optional: Override with specific values if needed
# host = {{env "PGHOST"}}
# port = {{env "PGPORT"}}
# database = {{env "PGDATABASE"}}
# user = {{env "PGUSER"}}
# password = {{env "PGPASSWORD"}}
# sslmode = {{env "PGSSLMODE"}}

# version_table = public.schema_version

[data]
# Custom variables available in migrations
app_name = civicrm
environment = development

